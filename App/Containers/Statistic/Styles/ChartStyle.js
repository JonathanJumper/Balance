import { StyleSheet } from "react-native"
import { Colors } from "../../../Themes"

export default StyleSheet.create({
  container: {
    flex: 42, // take 42% of the screen height
    backgroundColor: Colors.chartBg
  }
})